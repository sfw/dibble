# Current Backend Gap Analysis

This document compares the current Dibble backend against the authoritative planning packages:

- Product source of truth: `planning/4 - revised-spec/`
- Engineering handoff and migration guidance: `planning/5 - dev-handoff-revised-spec/`
- Historical context only: `planning/1`, `planning/2`, `planning/3`

## Status Summary

The backend now covers a meaningful slice of the revised Phase 1 and early Phase 2 foundation:

- LLM orchestration, retrieval-grounded generation, validation, streaming, auth, audit logging, observability, and provider resilience are implemented.
- The API surface now includes a single current route set that implements the revised generation/profile contract without carrying `v1` and `v2` path namespaces side by side.
- Generated content now has a persisted entity with quality and provenance metadata plus a cache-backed reuse path that supports explicit warming, calibration-aware predictive follow-up warming, a durable predictive warm queue with explicit processing, and overlap-based invalidation when learner evidence changes.
- The learner summary endpoint now packages recent calibration, durable progress-trend, durable learner-strategy, and activity context into a frontend-ready overview instead of requiring direct audit-log reads, and those same progress and strategy profiles now feed live router, generation-mode calibration, and predictive follow-up warming.
- Remediation is now session-backed: the remedial trigger persists a workflow session, generates the current workflow phase, exposes dedicated session retrieval and advancement endpoints for step-back, repair, and return progression, and now carries matched learner-strategy guidance through the session so cross-session struggle or regained independence influences later remediation steps directly.
- The backend now compacts recent run summaries plus progress snapshots into durable `learning.strategy.profile` events that expose honest heuristic long-horizon recovery, plateau, relapse, and volatility signals for similar future requests.
- The misconception pipeline now compacts repeated remediation signals into richer `learning.misconception.profile` events with recurrence counts, session counts, and recurrence labels such as `recurring` and `relapsing`, and remediation planning can now react to those durable recurrence signals instead of treating every misconception hit as isolated.
- Overlapping misconception matches on the same KC are now disambiguated into a single primary misconception signal per KC, with recurrence-aware profile evidence merged into the same signal before remediation focus and blueprint selection.
- Learner-strategy signals now also resolve into explicit per-KC sequencing actions such as `rebuild_prerequisite_first`, `hold_repair_target`, `hold_target`, and `attempt_transfer`, and those sequencing decisions now drive remediation workflow ordering plus predictive follow-up targeting.
- Recent `learner.observe`, `assessment.socratic`, and `content.generate` events for the same `learning_session_id` now feed a persisted within-session controller, so active sessions can carry explicit arc phases such as `stabilize`, `repair`, `consolidate`, `bridge`, and `transfer_check` plus recovery intent, streaks, generated-step counts, support-step budgets, loop-risk signals, and a concrete session `arc_action` across requests while same-session support and KC sequencing adapt before cross-session profile compaction runs.
- The backend now also compacts recent run summaries plus progress and learner-strategy snapshots into durable `learning.state.profile` events that calibrate affective, cognitive-load, and metacognitive targets across sessions, including recovery-stability, overload-risk, and metacognitive-reliability signals, and the live learner-state update path can blend those targets back into the next observation-driven state update when the durable match is strong enough.
- KC graph handling is now deeper than a flat prerequisite list: a reusable graph service provides multi-hop prerequisite and dependent traversal, same-LO sibling and bridge-candidate detection, distance-aware weighting, Socratic mastery migration can backfill missing KC mastery from stronger LO history, and affected LO mastery is now recomputed with weighted KC contributions instead of a flat mean.
- Knowledge Components now also carry light curated taxonomy metadata such as `concept_family`, `taxonomy_cluster_id`, and `nearby_kc_ids`, and the live KC graph can use those relations to surface honest local neighborhoods plus bridge candidates beyond same-LO siblings when sequencing remediation.
- Recent Socratic assessment turns now also persist discourse-aware follow-up state such as latest prompt style, next action, evidence strength, and a small `socratic_steering_action`, the turn policy can now deliberately re-probe from a new angle when clarification or step-back turns start to loop, and the live generation path can use that session-local conversational evidence plus the controller's newer `arc_action` to steer prompt variant choice, explanation-versus-practice mode selection, and next-step guidance more directly.
- The predictive warm queue now behaves more like a lightweight scheduler: queued tasks carry urgency classes plus retry metadata, claim selection preserves some batch space across urgency classes instead of starving lower-priority work forever, transient failures can defer with backoff, stale `processing` work can now be swept back into deferred retry instead of getting stranded after a crash, retry caps now vary by urgency class, and telemetry can now distinguish pending, deferred, retried, requeued, dropped, failed, canceled, and aged routine warm work more clearly.
- Practice generation now hydrates lightweight KC misconception hints directly from stored target KCs, so distractor guidance can explicitly target a known misconception and remediation move instead of relying only on generic distractor-style labels, now upgrades generic on-grade distractors into misconception-aware contrast when a live target misconception is known, and now carries named distractor slots plus answer-check focus; worked-example generation now carries named visible-step roles, hidden learner-step roles, a transfer move, an explicit step outline, and a learner-release plan rather than only a visible-step count.
- The generation engine now has an explicit moderation workflow separate from validation: it can flag unsafe learner requests before provider calls, re-check generated blocks before delivery, replace flagged request or response content with a teacher-safe fallback, surface richer moderation metadata such as matched terms on generation responses, emit explicit moderation events during streaming fallback, and write moderation state into generation audit and observability paths.
- The backend now also compacts recent learner observations plus durable state-profile context into `learning.cognitive_trait.profile` events, including trait-stability and challenge-tolerance signals, and live cognitive-trait inference can blend those cross-session trait targets back into processing-speed, working-memory, and spatial-reasoning updates when the durable profile is stable enough.
- Live learner-state and cognitive-trait inference now also score the strength of current evidence more explicitly, so rich contradictory current observations can push back harder on durable state/trait profiles while sparse evidence still lets strong durable profiles help with load, metacognition, and trait backfill.

The biggest remaining gaps are no longer basic plumbing. They are adaptive intelligence depth:

- true learner-state inference rather than manually supplied profile fields
- KC prerequisite graphs and misconception classification depth
- deeper generation-template selection and stronger learned adaptation beyond the current specialized modes
- broader cross-session adaptation depth beyond the current router, generation-mode, remediation, learner-summary, and recent recovery/independence strategy hooks
- richer scheduling and background execution beyond the current queue-backed but still lightweight predictive warm path

## Requirement Snapshot

Legend:

- `Implemented`: shipped in the current backend
- `Partial`: present in MVP form, but not at the depth expected by the revised spec
- `Missing`: still absent

| Requirement | Status | Notes |
|---|---|---|
| `LLM-001` LLM orchestration service | Implemented | Primary/secondary upstreams, failover, circuit breaker, selection strategies |
| `LLM-002` Prompt framework with versioning/A-B testing | Partial | Prompt registry now selects named templates with version and variant metadata for generation and Socratic assessment probes, observability now includes Socratic evidence aggregates plus per-template/style prompt-performance summaries, both assessment probes and the main experimented generation families can optionally prefer the stronger recent variant via audit-backed adaptive selection, generation variants can now incorporate aggregated observation-linked traces, same-session Socratic traces, later cross-generation session outcomes, and a newer session-local Socratic steering override that can directly switch generation prompt families after recent step-back, clarification, or transfer-confirmation turns, those traces are now condensed into explicit run-level outcome summaries plus confidence-weighted calibration signals, the summaries are now persisted as durable audit artifacts, prompt calibration now prefers those durable summaries before falling back to raw event-window reconstruction, router plus generation-mode calibration can now also prefer compact cross-session calibration, progress, learner-strategy, and recent conversational steering snapshots built from those summaries, and the live prompt builder now injects explicit distractor plans plus role-based worked-example fade plans derived from target KC hints, but experimentation is still minimal and there is no learned longer-horizon optimization loop |
| `LLM-003` RAG pipeline | Implemented | Hybrid lexical + embedding retriever with persistent embedding cache |
| `LLM-004` Safety/moderation layer | Partial | Validation and safety rules still exist, and there is now also an explicit moderation workflow that can flag unsafe requests or generated responses, cover a broader local category set such as abusive tone, academic-integrity prompts, privacy risk, sexual content, violence, and self-harm, replace flagged content with a teacher-safe fallback, emit explicit moderation events during streaming fallback, surface matched-term moderation metadata in generation responses, and persist moderation state into generation audits plus telemetry, but the moderation itself is still a local heuristic screen rather than a richer policy or classifier stack |
| `LLM-005` Streaming response architecture | Implemented | SSE route plus upstream chat-stream ingestion |
| `GEN-001` On-the-fly explanation generation | Implemented | Current unified generation routes produce grounded explanations |
| `GEN-002` Practice problem synthesis | Partial | Dedicated problem generation now carries difficulty-band metadata, trajectory-aware progression metadata such as progression action and distractor style, can hydrate lightweight target-KC misconception hints from the live catalog, now upgrades generic distractor selection into misconception-aware contrast when a target misconception is known, and now turns those hints into explicit distractor-focus, named distractor-slot, answer-check-focus, and remediation-anchor guidance for prompt/fallback generation, while persisted same-target calibration profiles, progress profiles, learner-strategy summaries, and within-session phase signals can still hold support, consolidate, bridge, or advance difficulty more intentionally than a one-step bias, but there is still no richer calibrated difficulty model or true distractor synthesis |
| `GEN-003` Worked example generation with fading | Partial | Dedicated worked-example generation now carries adaptive fading metadata, fade-focus and progression-action metadata, can be auto-selected from the unified generation path, and now names visible step roles, a hidden learner-owned step role, a target transfer move, a concrete step outline, and a learner-release plan so prompts/fallback generation can fade by role and transition instead of only visible-step count, while persisted same-target calibration profiles, progress profiles, learner-strategy summaries, and within-session phase signals can still hold full modeling, partially release support, bridge toward transfer, or accelerate fading more intentionally than a one-step bias, but the template library and fading progression are still conservative heuristics |
| `GEN-004` Remedial micro-module creation | Implemented | Remedial trigger now steps through prerequisite KCs, can consume catalogued KC misconception patterns, persists richer repeated misconception profiles with recurrence metadata, emits structured remediation blueprints that can prioritize recurring profile signals, can insert a same-LO bridge KC between repair and target return when the graph suggests a better local path, starts a persisted remediation workflow session, and advances multi-step `step_back`/`repair`/`bridge`/`return` generation through dedicated session state |
| `GEN-005` Multi-modal synthesis | Missing | No diagram, interactive, simulation, or code generation layer |
| `PROF-001` Cognitive trait assessment | Partial | Profile schema stores traits, the observation pipeline now refreshes lightweight processing-speed, working-memory, and spatial-reasoning estimates from recent interaction patterns, those live updates can now be blended with durable `learning.cognitive_trait.profile` snapshots compacted across sessions, that durable trait layer now includes stability and challenge-tolerance signals plus sharper mismatch gating, and the live trait path now also scores current challenge exposure and current evidence strength so rich contradictory low-support evidence can suppress even stable durable profiles while sparse evidence still benefits from strong durable backfill, but there is still no dedicated diagnostic assessment workflow or richer validation loop |
| `PROF-002` Affective state detection | Partial | Observation-driven inference now updates affective state with task-aware normalization, and a newer durable `learning.state.profile` layer can blend cross-session outcome-backed affective targets into live state inference and persistence while using newer recovery-stability, overload-risk, and confidence gating signals to avoid over-trusting optimistic history during sharply strained current observations, and the live state path now also scores current evidence strength and contradiction against the durable profile before blending, but the logic is still heuristic rather than a richer classifier |
| `PROF-003` Real-time cognitive load estimation | Partial | Observation-driven load inference now updates the profile with task-aware normalization, and the same durable `learning.state.profile` layer now adds cross-session outcome-backed load calibration plus explicit overload-risk signals before persistence while also being able to raise live load or capacity utilization when a strong durable overload profile still matches the learner, and the live state path now also backs off more when a richer set of current observations contradicts a lighter durable profile, but the model remains heuristic rather than learned |
| `PROF-004` KC granularity | Partial | KC mastery exists in the profile, there is now a persisted KC graph, a reusable graph service now supports multi-hop prerequisite/dependent traversal, same-LO sibling and bridge-candidate detection, curated local neighborhood relations via `concept_family`, `taxonomy_cluster_id`, and `nearby_kc_ids`, distance-aware weighting, and live sequencing/remediation can now use that richer local structure to distinguish prerequisite rebuild from nearby bridge KCs before target return, while Socratic mastery updates propagate through the same graph with LO-to-KC backfill and weighted LO recomputation, but the taxonomy is still only lightly curated and there is no broader expert-authored KC decomposition beyond the current stored entities |
| `PROF-005` Metacognitive tracking | Partial | Observation-driven confidence calibration and help-seeking signals now exist, are task-aware, influence routing/generation selection, can now be updated from Socratic assessment outcomes, now also receive conservative run-summary adjustments in both router feedback and learner-state persistence, are paired with a durable long-horizon learner-strategy profile that summarizes recovery, plateau, relapse, volatility, and independence needs across sessions, and now also reuse a durable `learning.state.profile` layer with metacognitive-reliability evidence, current-evidence scoring, and stronger contradiction gating to blend cross-session metacognitive targets into live state updates and learner summaries, but the modeling remains heuristic rather than learned |
| `ADAPT-001` Thompson Sampling router | Implemented | Thompson-style policy with safety constraints is in the production path, and a calibration wrapper now prefers compact cross-session progress profiles, then calibration profiles, then recent same-target run summaries, back into final support selection |
| `ADAPT-002` Within-session adaptation | Partial | Streaming generation exists, and recent same-session observation plus Socratic evidence now update a persisted within-session controller that can carry explicit arc phases such as `stabilize`, `repair`, `consolidate`, `bridge`, and `transfer_check` plus recovery intent, streaks, generated-step counts, support-step budgets, loop-risk signals, and a concrete next `arc_action` across an active learning session while immediately raising or relaxing support, pacing transfer, varying prompt guidance, and holding or advancing KC sequencing on the next request, but the controller is still heuristic and there is still no richer learned long-arc planner or continuous adaptive loop |
| `ADAPT-003` Misconception detection/classification | Partial | Misconception detection now combines rule-based mastery gaps with optional KC-level misconception catalogs, alias-aware evidence matching, graph-weighted prerequisite-gap scoring, confidence scoring, remediation hints, recommended repair targets, durable repeated-misconception profile signals from prior remediation runs, recurrence-aware profile/catalog signal merging, and per-KC primary misconception disambiguation with distinctive-evidence weighting before remediation selection, but it is still a lightweight heuristic taxonomy rather than a learned classifier |
| `ADAPT-004` Automatic step-back intervention | Implemented | Router and generation path support step-back content generation |
| `ADAPT-005` Conversational/Socratic assessment | Partial | A persisted Socratic assessment flow now scores the current learner response with modular evidence dimensions, stores multi-turn session state, chooses follow-up prompt style with a more discourse-aware turn policy that can react to recovery after step-back and failed transfer checks, can now deliberately break clarification or step-back loops with a fresh diagnostic angle, feeds outcomes back into learner-profile mastery and metacognitive state, and can now contribute same-session plus later-session evidence to explicit generation run summaries and calibration signals that now influence router support, learner-state persistence, prompt calibration, generation-mode selection, prompt-family steering, durable learner progress summaries, an explicit KC-sequencing layer, and a durable learner-strategy layer used by live remediation and support-fading decisions, but it still lacks richer learned discourse modeling across full learning traces |
| `API-001` `POST /api/content/generate` | Implemented | Current unified generation endpoint returns persisted generated-content metadata |
| `API-002` `POST /api/remedial/trigger` | Implemented | Starts a persisted remediation workflow session, returns richer misconception and workflow metadata, and is now paired with remediation-session retrieval and advancement endpoints for longer-running orchestration |
| `API-003` `GET /api/learners/{id}/profile` | Implemented | Current unified learner-profile endpoint returns the extended dimensions |
| `API-004` `POST /api/llm/stream` | Implemented | Current unified streaming surface |
| `DATA-001` KnowledgeComponent entity and prerequisite graph | Implemented | Persisted KC entity plus prerequisite traversal API and remediation-planner integration |
| `DATA-002` Extended learner profile | Implemented | Current profile model includes cognitive, affective, load, and preference dimensions |
| `DATA-003` GeneratedContent entity with quality metadata | Implemented | Persisted generated content plus `generation_metadata` and `GeneratedContent` API envelope |
| `INFRA-003` Pre-generation and intelligent caching | Partial | Request-time generation cache and an explicit warmup endpoint now have calibration-aware anticipatory follow-up warming, predictive request metadata, a durable SQLite-backed predictive warm queue, explicit queue processing, overlap-based invalidation from observation and Socratic outcomes, telemetry coverage, newer strategy-aware escalation for relapse or plateau follow-ups, phase-aware follow-up selection that can keep a session in consolidate or bridge before transfer, queue-side priority plus stale-task expiry so the most urgent likely-next steps are warmed first, bounded urgency-class batch selection so routine work is not starved by repeated urgent bursts, retry/backoff handling for transient warm failures, stale-processing sweep-and-requeue recovery after interrupted work, and urgency-class retry caps so routine work fails faster while urgent work gets one more chance, but there is still no broader autonomous scheduler or richer learned prediction policy |

## Highest-Value Next Gaps

Based on `planning/4 - revised-spec/implementation-roadmap.md` and `planning/5 - dev-handoff-revised-spec/requirements-traceability.csv`, the strongest next backend slices are:

1. deeper `PROF-001`/`PROF-002`/`PROF-003`/`PROF-005`: keep extending the new durable learner-state and cognitive-trait calibration layers beyond the current cross-session recovery, overload, stability, and challenge-tolerance heuristics into stronger outcome-backed models and richer trait evidence.
2. `GEN-002` + `GEN-003`: extend the new misconception-aware distractor guidance and role-based fade plans into richer calibrated problem construction, stronger transfer moves, and a broader template bank.
3. `LLM-004`: deepen the new heuristic moderation workflow into stronger policy coverage, richer categories, and better stream-time handling if/when the product needs stricter real-time moderation.
4. keep extending the new predictive scheduler from queue robustness into more autonomous background execution and stronger learned next-step prediction once more outcome data exists.
5. keep extending the new Socratic steering hooks from prompt-family and next-step heuristics into longer-run conversational adaptation once real traces exist.

## Recommendation

The most coherent next implementation step is now:

- deepen the learner-strategy, durable learner-state and cognitive-trait calibration, KC-graph sequencing, and persisted within-session controller layers beyond the current recovery, plateau, relapse, volatility, overload, stability, per-KC misconception-disambiguation, first-pass KC-sequencing, and phase or streak-based session heuristics
- replace more of the remaining heuristic learner-state, trait, and misconception logic with better-calibrated models trained from real outcomes
- keep building on the current predictive cache path rather than introducing a separate orchestration stack too early

That is now more achievable because the backend no longer relies only on explicit warmup calls or raw event-window reconstruction. The generation path can now warm likely next steps during normal content delivery, tag those warmed entries as predictive without fragmenting cache reuse, adapt those warmed follow-ups when route or mode calibration suggests more or less support, escalate negative cross-session practice toward prerequisite repair, break plateaus with varied modeled support, enqueue those next steps durably for later processing, expire both cached entries and queued tasks when new same-target learner evidence arrives, and expose that behavior in telemetry. On the adaptation side, Socratic assessment and learner observations already feed learner-state updates, within-step trace aggregation, later same-session outcome tracing, explicit run-level calibration summaries, persisted run-summary audit events, compact cross-session calibration-profile and progress-profile events, a conservative router calibration layer that can now react to improving or declining cross-session trends, a new learner-strategy layer that compacts those same signals into reusable recovery, plateau, relapse, volatility, and independence guidance, a persisted within-session controller that now keeps phase, recovery intent, streaks, and generated-step counts alive across active session steps, a newer durable learner-state profile layer that turns the same history into affective/load/metacognitive calibration targets for later observation updates and learner-summary packaging, a newer durable cognitive-trait profile layer that turns recent observation history plus state-profile context into reusable processing-speed, working-memory, and spatial-reasoning targets for later profile updates, a richer KC graph and migration layer that can now propagate mastery across weighted prerequisite and dependent hops while backfilling missing KC coverage from LO history, a richer misconception-profile layer that now compacts recurrence across remediation sessions and can upgrade a current request into a recurring or relapsing pattern when evidence overlaps, prompt-selection plus telemetry paths that now reuse those persisted summaries before raw reconstruction, and remediation workflows that can now persist and advance multi-step recovery sessions instead of collapsing remediation into a single response. The next coherent step is to deepen those strategy, state, trait, controller, and graph-aware sequencing layers rather than jumping to a separate orchestration stack or pretending the current heuristics are already learned models.
